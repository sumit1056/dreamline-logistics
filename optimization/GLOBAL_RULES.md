# GLOBAL AGENT DIRECTIVES (`S:\optimization\MASTER_TOOL_CATALOG.md`)

1. **Roles & Pre-Vibecoding Architecture**: Partner mode. Verify 6 Pre-Vibecoding Docs (`PRD`, `TRD`, `UI_UX_DESIGN`, `APPFLOW`, `BACKEND_SCHEMA`, `IMPLEMENTATION_PLAN`) & Spec Kit (`/speckit.specify`, `/speckit.plan`, `/speckit.tasks`).
2. **Anti-Drift & Refresh**: Re-read `KNOWLEDGE/SPEC.md` & `CORE_MEMORIES.md` before high-impact changes.
3. **Scope-Guard**: Flag out-of-scope requests with `🚨 SCOPE CHANGE DETECTED`. Update `AUDIT_LOG.md`.
4. **Design Quad & Dynamic Skill Discovery**: For UI, run `npx skills add Leonxlnx/taste-skill pbakaus/impeccable emilkowalski/skill nextlevelbuilder/ui-ux-pro-max-skill/ui-ux-pro-max vercel-labs/skills/find-skills`. Apply `TasteSkill`, `Impeccable` (`/polish`, `/distill`), `Emil Kowalski` (`animate`), `UI-UX Pro Max`, and `find-skills` (`skills.sh`).
5. **Context7 Guard**: Free quota (1,000/mo). Use ONLY for `use context7` or complex 3rd-party APIs (Stripe, Next 15).
6. **Sentry MCP**: Query `mcp.sentry.dev` ON-DEMAND ONLY during bug triage.
7. **Cost Guard**: All tools must be 100% free-tier compliant. Never run paid APIs in background loops.
8. **Reticle QA**: Verify web flows with `reticle.sh` before claiming success.
9. **Compatibility Guard**: Alert with `⚠️ TOOL / MCP COMPATIBILITY WARNING` before choices that break installed tools.
10. **Sequential Thinking**: Use `sequentialthinking` MCP for complex architecture & deep debugging.
11. **Multi-Machine MCP Check**: Check `mcp_config.json` on startup. If core servers are missing, alert with `⚠️ MISSING MCP SETUP DETECTED`.
12. **Traceability Protocol**: Plan first, maintain `DECISIONS.md`, `FLOW.md`, `ARCHITECTURE.md`, `CONSTRAINTS.md`, comment inline intent, end with 5-line handoff.
13. **31-Point Production Audit**: Run SEO, single H1, rate-limiting, Zod schemas, zero secret leaks, gzip, batch inserts, circuit breakers, optimistic UI, and fragment caching before launch.
14. **Proactive Catalog Matcher**: Cross-reference `S:\optimization\MASTER_TOOL_CATALOG.md` and prompt with `💡 PROACTIVE TOOL SUGGESTION`.
